sourceCompatibility = targetCompatibility = 8

dependencies {
    compile 'org.reactivestreams:reactive-streams'
    compile 'io.cloudevents:cloudevents-api:2.2.0'

    testCompile 'org.assertj:assertj-core'
}
